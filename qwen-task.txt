I have indexed this repo for you in codebase-memory-mcp.
codebase-memory-mcp cli list_projects
Preparing one-shot local CBM command...
level=info msg=version_cohort.claimed_unheld build=1175645cb30560e7e47d78611cd1bcb509478eaf6d4e51f72fe18327ee9c1351
level=info msg=mem.allocator.owned classes=all
level=info msg=mem.init budget_mb=290173 total_ram_mb=580346 source=ram_fraction
Running list_projects locally...
hint: this command started a temporary CBM daemon. `codebase-memory-mcp daemon start` keeps one warm and removes this startup cost from every CLI command.
{"projects":[{"name":"data-redless-workspaces-redless-clone","root_path":"/data/redless-workspaces/redless-clone"}],"total":1,"offset":0,"limit":50,"returned":1,"has_more":false}
Completed list_projects (4210 ms)

You can use `codebase-memory-mcp --help` to learn how to use it. Use its tools to help you get a better understanding of the codebase before you begin traversing the filesystem.

Remove the Nemotron-specific tool protocol from Redless with the smallest safe diff possible.

This is a surgical cleanup task, not a refactor.

Remove only code, configuration, documentation, and tests that exist specifically to support the Nemotron textual tool-call protocol.

Preserve all generic llama.cpp behavior and all unrelated functionality.

In particular, do not change:

HTTP authentication behavior
request timeout behavior
streaming/progress formatting
response-streaming validation
constructor/configuration structure unless required solely to remove Nemotron
generic SSE parsing behavior
generic tool-call parsing/finality behavior
generic tests
unrelated model catalogs/provider data
unrelated docs or configuration

Preserve the existing OpenAI/native tool-call path exactly except where a tiny change is required because the Nemotron branch is being removed.

Do not perform opportunistic cleanup, simplification, modernization, style refactoring, API redesign, or unrelated type changes.

Before editing:

identify every Nemotron-specific reference;
distinguish Nemotron-specific code from generic code that happened to be added during the same development work;
inspect the relevant tests before deciding what to delete.

After editing:

search for remaining Nemotron-specific references;
restore any unrelated changes;
run the relevant model and agent tests;
run the broader available test suite;
inspect git diff carefully;
verify that every changed line is necessary for removing Nemotron support.

Do not add the task prompt itself to the repository.
Do not stage, commit, merge, or push changes.

The task is complete when Nemotron-specific support is gone, native/OpenAI-style tool calling still works, generic behavior is preserved, tests pass, and the final diff contains no unrelated changes.
